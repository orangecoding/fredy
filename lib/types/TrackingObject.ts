export interface TrackingObject {
  name?: string;
  provider: string[];
  adapter: string[];
  isDemo: boolean;
  operating_system: string;
  os_version: string;
  arch: string;
  nodeVersion: string;
  language: string;
  distinct_id: string;
  fredy_version: string;
}
